version: '3.8'
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: proyectoDO
      POSTGRES_DB: door_drop_db
    ports:
      - "5432:5432"
